extends Node

# Хранит состояние дорожек на уровне: есть ли на них Spawn или нет.
# warning-ignore:UNUSED_CLASS_VARIABLE
var roads_ids := {}

# Ловец душ проверяет, на скольких дорожках ему нужно дождаться блок.
# warning-ignore:UNUSED_CLASS_VARIABLE
var roads_quantity := 0.0
